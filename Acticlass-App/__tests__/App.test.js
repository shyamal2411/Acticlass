/**
 * @format
 */

import 'react-native';

// Note: import explicitly to use the types shiped with jest.
import { test } from '@jest/globals';

test('Simple test 1 + 1 = 2', () => {
  expect(1 + 1).toBe(2);
});

